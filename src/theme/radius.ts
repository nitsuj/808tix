/**
 * 808Tix border radius tokens.
 */

export const radius = {
  input: 12,
  button: 14,
  card: 16,
  /** Floating guest pass / wallet credential card. */
  walletCard: 24,
  statusBanner: 12,
  badge: 999,
} as const;
