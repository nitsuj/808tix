import { PrototypeDashboardShell } from '@/components/design-prototype/prototype-dashboard-shell';

/**
 * Internal design prototype only.
 * Route: /design/admin-dashboard-prototype
 * Direct URL — no public nav link.
 */
export default function AdminDashboardPrototypeScreen() {
  return <PrototypeDashboardShell />;
}
