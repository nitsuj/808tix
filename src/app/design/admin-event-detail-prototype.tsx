import { PrototypeEventDetailShell } from '@/components/design-prototype/prototype-event-detail-shell';

/**
 * Internal design prototype only.
 * Route: /design/admin-event-detail-prototype
 * Direct URL — no public nav link.
 */
export default function AdminEventDetailPrototypeScreen() {
  return <PrototypeEventDetailShell />;
}
