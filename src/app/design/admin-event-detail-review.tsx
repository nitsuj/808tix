import { AdminEventDetailReviewShell } from '@/components/design-review/admin-event-detail-review-shell';

/**
 * Internal design-review route only.
 * Route: /design/admin-event-detail-review
 * Direct URL — no public nav link. No auth. Mock data only.
 */
export default function AdminEventDetailReviewScreen() {
  return <AdminEventDetailReviewShell />;
}
